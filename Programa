#include <stdio.h>
#include <stdlib.h>

int main()
{
    char nome_do_livro[100], editora[100], autor[100], data_locacao[15],data_devolucao[15],nome_aluguel[100];
    int quantidade,status,cod_prateleira,cod_livro;

    printf("\nCADASTRO DE LIVRO\n\n\n");

    printf("Digite o nome do livro: ");
    gets(nome_do_livro);

    printf("\nDigite o nome da editora: ");
    gets(editora);

    printf("\nDigite o nome do autor: ");
    gets(autor);

    printf("\nDigite o codigo da prateleira: ");
    scanf("%d",&cod_prateleira);

    printf("\nDigite o codigo do livro: ");
    scanf("%d",&cod_livro);

    printf("\nDigite a quantidade disponivel: ");
    scanf("%d",&quantidade);
}
